<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.paularagones.moode" >

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".Application.MooderApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/MooderTheme" >

        <activity
            android:name=".Activities.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.StatusListActivity"
            android:label="@string/status_list_activity" >
        </activity>
        <activity
            android:name=".Activities.DateActivity2"
            android:label="@string/title_activity_date" >
        </activity>
        <activity
            android:name=".Activities.MoodActivity"
            android:label="@string/title_activity_mood" >
        </activity>
        <activity
            android:name=".Activities.AddNewStatusActivity">
        </activity>
        <activity
            android:name=".Activities.AddNewPersonActivity">
        </activity>
        <activity
            android:name=".Activities.AddNewFeelingsActivity">
        </activity>
        <activity
            android:name=".Activities.AddNewLocationActivity">
        </activity>
        <activity
            android:name=".Activities.AddNewActivity">
        </activity>
    </application>

</manifest>
